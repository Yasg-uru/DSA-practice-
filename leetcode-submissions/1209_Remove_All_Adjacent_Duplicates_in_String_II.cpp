class Solution {
public:
    string removeDuplicates(string s, int k) {
       unordered_map<char,int>mp;
       for(int i=0;i<s.size();++){
           mp[s[i]]++;
       }
       //stack<char>st;
      
    }
};